apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: dev-

commonLabels:
  environment: development

resources:
  - ../../base

patches:
  - path: configMap.yaml
  - path: route.yaml
